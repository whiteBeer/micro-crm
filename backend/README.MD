#### Setup

1. Add .env file in the root;
2. Add MONGO_URI;
3. Add JWT_SECRET (https://www.npmjs.com/package/jsonwebtoken);
4. Add JWT_LIFETIME (optional, 1h by default).
5. Add PORT (optional, 3000 by default)

```bash
npm install && npm run build && npm start
```
